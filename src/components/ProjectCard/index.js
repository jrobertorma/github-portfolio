import React from 'react';

const ProjectCard = ({projectData}) => {
    //console.log(projectData);
    return ( <div>YOYOYO, soy project card: {projectData.name}</div> );
}
 
export default ProjectCard;